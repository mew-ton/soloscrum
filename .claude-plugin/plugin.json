{
  "name": "soloscrum",
  "description": "One-person AI-driven development framework. Automates the cycle from idea to GitHub Issue, task decomposition, implementation, and review. Supports github-only or linear+github tracker profiles.",
  "version": "0.2.0",
  "author": {
    "name": "mew-ton"
  },
  "repository": "https://github.com/mew-ton/soloscrum",
  "userConfig": {
    "tracker_profile": {
      "type": "string",
      "title": "Tracker Profile",
      "description": "Where SP, dependencies, and state are stored. Use 'github-only' (default) for repos restricted to GitHub Issues, or 'linear+github' for repos with Linear native sync configured.",
      "default": "github-only"
    },
    "worktree_root": {
      "type": "string",
      "title": "Worktree Root",
      "description": "Repo-root-relative directory where /soloscrum:develop creates each work unit's git worktree. Must stay inside the repository. Override per-repo with worktree_root in .claude/rules/branch.md frontmatter.",
      "default": ".soloscrum/worktrees"
    }
  }
}
